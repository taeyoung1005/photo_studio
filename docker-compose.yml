services:
  django:
    build: .
    ports:
      - "8000:8000"
    restart: always
  
  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - django
    restart: always